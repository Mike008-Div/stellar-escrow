/**
 * Stellar Escrow SDK — placeholder.
 *
 * Planned exports:
 *   - createEscrow(params)
 *   - fundEscrow(id)
 *   - release(id)
 *   - refund(id)
 *   - raiseDispute(id, raisedBy)
 *   - resolveDispute(id, winner)
 */
export const VERSION = "0.0.1";
